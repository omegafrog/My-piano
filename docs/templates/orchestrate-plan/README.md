# Orchestrate-Plan Document Templates

This folder provides reusable markdown templates aligned with the workflow contract.

Core rules:
- Use `doc_path` in `# Properties`.
- Do not use document ids.
- Keep canonical files even when a specific document is not substantively applicable.
- Prefer grep-based discovery over ad-hoc navigation.
